import UpdatePrompt from "@components/UpdatePrompt";
import { Suspense } from "react";

const EditPrompt = () => {
  return (
    <Suspense>
      <UpdatePrompt />
    </Suspense>
  );
};

export default EditPrompt;
